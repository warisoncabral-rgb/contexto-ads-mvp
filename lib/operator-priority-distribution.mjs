const priorities=['critical','high','normal']
export function deriveOperatorPriorityDistribution(queue){return {distribution:priorities.map((priority)=>({priority,count:queue.items.filter((item)=>item.priority===priority).length,tenantCount:new Set(queue.items.filter((item)=>item.priority===priority).map((item)=>item.tenantId)).size})),boundaries:{riskScoreInvented:false,deadlineInferred:false,externalWritesPerformed:false}}}
