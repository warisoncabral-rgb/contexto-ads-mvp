export function deriveOperatorTenantCoverage(queue){const itemsByTenant=new Map();for(const item of queue.items)itemsByTenant.set(item.tenantId,(itemsByTenant.get(item.tenantId)??0)+1);return {tenants:queue.snapshots.map(snapshot=>({tenantId:snapshot.tenantId,itemCount:itemsByTenant.get(snapshot.tenantId)??0,queueDate:snapshot.queueDate,hasPendingWork:(itemsByTenant.get(snapshot.tenantId)??0)>0,baselineAvailable:snapshot.comparison?.baselineAvailable??false})).sort((a,b)=>a.tenantId.localeCompare(b.tenantId)),authorizedTenantCount:queue.snapshots.length,tenantsWithoutPendingWork:queue.snapshots.filter(snapshot=>(itemsByTenant.get(snapshot.tenantId)??0)===0).length,boundaries:{zeroItemsTreatedAsAbsence:false,tenantAuthorizationInferred:false,externalWritesPerformed:false}}}
