const statuses=['included','deferred','ignored']
export function deriveOperatorSourceDecisionMatrix(queue){const sources=new Map();for(const snapshot of queue.snapshots){for(const decision of snapshot.sourceDecisions){const row=sources.get(decision.source)??{source:decision.source,included:0,deferred:0,ignored:0,tenantCount:0,tenantIds:new Set()};row[decision.status]+=1;row.tenantIds.add(snapshot.tenantId);sources.set(decision.source,row)}}return {sources:[...sources.values()].map(({tenantIds,...row})=>({...row,tenantCount:tenantIds.size})).sort((a,b)=>a.source.localeCompare(b.source)),statuses,boundaries:{sourceQualityInferred:false,availabilityInferred:false,externalWritesPerformed:false}}}
