CONTEXT_ADS_API_BASE_URL=http://localhost:3001
# Mesmo token usado para gerar OPERATOR_BOOTSTRAP_TOKEN_SHA256 no backend.
# Server-side somente: nunca use prefixo NEXT_PUBLIC_ e nunca salve o valor no Git.
CONTEXT_ADS_OPERATOR_TOKEN=
