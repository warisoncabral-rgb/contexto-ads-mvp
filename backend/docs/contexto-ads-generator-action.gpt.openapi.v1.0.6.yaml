openapi: 3.1.0
info:
  title: Contexto Ads Generator Integration V1
  version: 1.0.6
  description: >-
    GPT-compatible no-write campaign handoff with automatic tenant/Meta target resolution
    and explicit timeout recovery. No operation in this Action authorizes Meta delivery,
    spend, activation, publication or external writes.
servers:
  - url: https://contexto-ads-validation-api.onrender.com/v1
security:
  - bearerAuth: []
paths:
  /operator/campaign-packages/v1/submit:
    post:
      operationId: submitCampaignPackage
      summary: Submit a completed Campaign Package V1
      description: >-
        Preferred campaign handoff. The Generator authenticates the operator, resolves the
        authorized campaign-preparation tenant and selected Meta target, injects technical IDs,
        validates and persists the package. Performs no Meta write. IMPORTANT: create and retain
        package_id before calling this operation. If this call ends with ClientResponseError,
        timeout, connection error or an uncertain client-side result, DO NOT generate a new
        package_id and DO NOT blindly resubmit. First call recoverCampaignPackageStatus with the
        exact same package_id. Resubmit only if recovery confirms the package is not persisted,
        and then reuse the exact same package_id, package_version and content.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CampaignPackageAutoV1'
      responses:
        '201':
          description: Handoff accepted and persisted with technical target resolved internally
        '400':
          description: Campaign Package invalid
        '401':
          description: Authentication denied
        '404':
          description: No ready selected Meta target
        '409':
          description: Tenant/target ambiguity or version/content conflict

  /operator/campaign-packages/v1/{packageId}/status:
    get:
      operationId: recoverCampaignPackageStatus
      summary: Recover Campaign Package status after an uncertain submit result
      description: >-
        Read-only recovery operation designed for ClientResponseError, timeout or lost submit
        responses. It resolves the authorized tenant internally from the authenticated operator
        and searches for the exact package_id. Call this before any submit retry. A 200 proves the
        package exists and returns its current internal state. A 404 means it was not found in the
        operator's authorized campaign-preparation tenants. Performs no Meta write.
      parameters:
        - name: packageId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Package found; current state returned
        '401':
          description: Authentication denied
        '404':
          description: Package not found
        '409':
          description: Package status is ambiguous across authorized tenants

  /operator/tenants/{tenantId}/campaigns/{campaignId}/plans/{executionPlanId}/creative-packages:
    post:
      operationId: submitReviewedCreativePackage
      summary: Submit reviewed creative content
      description: Persists reviewed creative content only; performs no Meta write.
      parameters:
        - name: tenantId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: campaignId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: executionPlanId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [creative]
              properties:
                creative:
                  $ref: '#/components/schemas/CreativePackageInputV1'
      responses:
        '201': { description: Creative version created }
        '400': { description: Invalid payload }
        '401': { description: Authentication denied }
        '404': { description: Plan not found }
        '409': { description: Conflict }

  /operator/tenants/{tenantId}/campaigns/{campaignId}/creative-packages/latest:
    get:
      operationId: getLatestCreativePackage
      summary: Get latest creative package
      parameters:
        - name: tenantId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: campaignId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200': { description: Latest creative package }
        '401': { description: Authentication denied }
        '404': { description: Creative package not found }

  /operator/tenants/{tenantId}/campaigns/{campaignId}/creative-packages/{version}/approve:
    post:
      operationId: approveCreativePackage
      summary: Approve exact creative version and content hash
      description: Creative approval is not plan approval or execution authorization.
      parameters:
        - name: tenantId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: campaignId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: version
          in: path
          required: true
          schema: { type: integer, minimum: 1 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [contentHash]
              properties:
                contentHash:
                  type: string
                  pattern: '^[0-9a-f]{64}$'
      responses:
        '201': { description: Creative approved }
        '400': { description: Invalid request }
        '401': { description: Authentication denied }
        '404': { description: Creative package not found }
        '409': { description: Stale creative or invalid transition }

  /operator/tenants/{tenantId}/campaigns/{campaignId}/plans/{executionPlanId}/approvals:
    post:
      operationId: requestExecutionPlanApproval
      summary: Request approval for the exact current execution plan
      description: Requesting plan approval performs no Meta write and is not execution authorization.
      parameters:
        - name: tenantId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: campaignId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: executionPlanId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '201': { description: Approval requested }
        '401': { description: Authentication denied }
        '404': { description: Plan not found }
        '409': { description: Plan stale or not approvable }

  /operator/tenants/{tenantId}/approvals/{approvalId}:
    get:
      operationId: getExecutionPlanApproval
      summary: Get current execution plan approval state
      parameters:
        - name: tenantId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: approvalId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200': { description: Approval state }
        '401': { description: Authentication denied }
        '404': { description: Approval not found }

  /operator/tenants/{tenantId}/approvals/{approvalId}/{decision}:
    post:
      operationId: decideExecutionPlanApproval
      summary: Record explicit plan approval decision
      description: >-
        Approve, reject or revoke the exact plan approval. Approval is not execution authorization
        and this operation performs no Meta write.
      parameters:
        - name: tenantId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: approvalId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: decision
          in: path
          required: true
          schema:
            type: string
            enum: [approve, reject, revoke]
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                reason: { type: string, maxLength: 1000 }
      responses:
        '201': { description: Decision recorded }
        '400': { description: Invalid decision }
        '401': { description: Authentication denied }
        '404': { description: Approval not found }
        '409': { description: Invalid transition }

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
  schemas:
    CampaignPackageAutoV1:
      type: object
      additionalProperties: false
      required:
        - package_id
        - package_version
        - created_at
        - source
        - client_id
        - business_name
        - business_description
        - offer_name
        - offer_description
        - offer_type
        - campaign_objective
        - conversion_destination
        - campaign_goal_description
        - audience_description
        - locations
        - budget_type
        - budget_amount
        - currency
        - duration_days
        - ads
        - media
        - strategy_status
        - handoff_status
      properties:
        package_id: { type: string, format: uuid }
        package_version: { type: integer, minimum: 1 }
        created_at: { type: string, format: date-time }
        source: { type: string, enum: [contexto_ads] }
        client_id: { type: string, minLength: 1 }
        business_name: { type: string, minLength: 1 }
        business_description: { type: string, minLength: 1 }
        offer_name: { type: string, minLength: 1 }
        offer_description: { type: string, minLength: 1 }
        offer_type:
          type: string
          enum: [product, service, catalog, promotion, lead_generation]
        commercial_conditions:
          type: object
          additionalProperties: true
        campaign_objective: { type: string, enum: [LEADS] }
        conversion_destination: { type: string, enum: [WHATSAPP] }
        campaign_goal_description: { type: string, minLength: 1 }
        audience_description: { type: string, minLength: 1 }
        locations:
          type: array
          minItems: 1
          items:
            type: object
            additionalProperties: false
            required: [city, country]
            properties:
              city: { type: string, minLength: 1 }
              state: { type: string }
              country: { type: string, minLength: 1 }
              radius_km: { type: number, exclusiveMinimum: 0 }
              include: { type: boolean }
        age_min: { type: integer, minimum: 18, maximum: 65 }
        age_max: { type: integer, minimum: 18, maximum: 65 }
        gender: { type: string, enum: [all, male, female] }
        targeting_notes:
          type: object
          additionalProperties: true
        budget_type: { type: string, enum: [DAILY, LIFETIME] }
        budget_amount: { type: number, exclusiveMinimum: 0 }
        currency: { type: string, pattern: '^[A-Z]{3}$' }
        duration_days: { type: integer, minimum: 1, maximum: 365 }
        start_date: { type: string }
        end_date: { type: string }
        ads:
          type: array
          minItems: 1
          maxItems: 3
          items:
            type: object
            additionalProperties: false
            required: [ad_reference, primary_text, headline, cta, initial_message, media_id]
            properties:
              ad_reference: { type: string, minLength: 1 }
              primary_text: { type: string, minLength: 1 }
              headline: { type: string, minLength: 1 }
              description: { type: string }
              cta: { type: string, enum: [WHATSAPP_MESSAGE] }
              initial_message: { type: string, minLength: 1 }
              media_id: { type: string, minLength: 1 }
        media:
          type: array
          minItems: 1
          items:
            type: object
            additionalProperties: false
            required: [media_id, media_type, source, file_reference, checksum, mime_type, width, height]
            properties:
              media_id: { type: string, minLength: 1 }
              media_type: { type: string, enum: [image] }
              source: { type: string, minLength: 1 }
              file_reference: { type: string, minLength: 1 }
              checksum: { type: string, pattern: '^(sha256:)?[0-9a-fA-F]{64}$' }
              mime_type: { type: string, enum: [image/jpeg, image/png] }
              width: { type: integer, minimum: 1 }
              height: { type: integer, minimum: 1 }
        constraints:
          type: object
          additionalProperties: true
        strategy_notes:
          type: object
          additionalProperties: true
        strategy_status: { type: string, enum: [COMPLETE] }
        handoff_status: { type: string, enum: [READY_FOR_GENERATOR] }
        meta_connection_id: { type: string, format: uuid }
        ad_account_id: { type: string }
        facebook_page_id: { type: string }
        instagram_account_id: { type: string }
        whatsapp_asset_id: { type: string }

    CreativePackageInputV1:
      type: object
      additionalProperties: false
      required: [copies, claims, assets, reviewChecklist]
      properties:
        copies:
          type: array
          minItems: 1
          items:
            type: object
            additionalProperties: false
            required: [copyId, primaryText, headline, whatsappMessage, callToAction]
            properties:
              copyId: { type: string }
              primaryText: { type: string }
              headline: { type: string }
              description: { type: string }
              whatsappMessage: { type: string }
              callToAction: { type: string, enum: [SEND_WHATSAPP_MESSAGE] }
        claims:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [claimId, text, sourceRefs]
            properties:
              claimId: { type: string }
              text: { type: string }
              sourceRefs:
                type: array
                items: { type: string }
        assets:
          type: array
          minItems: 1
          items:
            type: object
            additionalProperties: false
            required: [assetId, storageRef, sha256, mimeType, width, height]
            properties:
              assetId: { type: string }
              storageRef: { type: string }
              sha256: { type: string, pattern: '^[0-9a-f]{64}$' }
              mimeType: { type: string, enum: [image/jpeg, image/png] }
              width: { type: integer, minimum: 1 }
              height: { type: integer, minimum: 1 }
        reviewChecklist:
          type: object
          additionalProperties: false
          required:
            - claimsVerifiedAgainstSources
            - visualFidelityReviewed
            - safeAreaReviewed
            - requiredFieldsReviewed
            - automaticEnhancementsReviewed
          properties:
            claimsVerifiedAgainstSources: { type: boolean }
            visualFidelityReviewed: { type: boolean }
            safeAreaReviewed: { type: boolean }
            requiredFieldsReviewed: { type: boolean }
            automaticEnhancementsReviewed: { type: boolean }
