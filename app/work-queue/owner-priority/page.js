import { loadOperatorWorkQueue } from '../../../lib/operator-work-queue.mjs'
import { deriveOperatorOwnerPriorityMatrix } from '../../../lib/operator-owner-priority-matrix.mjs'
const labels={operator:'Operador',system:'Sistema',meta_environment:'Ambiente Meta'}
export default async function OwnerPriorityPage(){const result=await loadOperatorWorkQueue();if(result.kind!=='ready')return <main className="portfolio-state"><span className="eyebrow">Responsabilidade × prioridade</span><h1>Dados não confirmados.</h1><a href="/work-queue/overview">Voltar à central</a></main>;const view=deriveOperatorOwnerPriorityMatrix(result.queue);return <><header className="topbar"><a className="brand" href="/"><span className="brand-mark">C</span><span><strong>Contexto Ads</strong><small>Responsabilidade × prioridade</small></span></a><div className="environment"><span />Somente leitura</div></header><main className="work-shell"><section className="work-hero"><div><span className="eyebrow">Matriz operacional</span><h1>Como as prioridades se distribuem por responsável.</h1><p>A matriz cruza apenas os campos persistidos de responsável e prioridade.</p></div><a href="/work-queue/overview">Central de comando</a></section><section className="work-list">{view.rows.map(row=><article className="work-item" key={row.owner}><div className="work-item-head"><div><span>{row.tenantCount} cliente(s)</span><h3>{labels[row.owner]}</h3></div></div><p>Crítica {row.counts.critical} · Alta {row.counts.high} · Normal {row.counts.normal}</p></article>)}</section><div className="portfolio-boundary">Responsabilidade não é convertida em controle, autorização ou score de risco.</div></main></>}
