import { loadOperatorWorkQueue } from '../../../lib/operator-work-queue.mjs'
import { deriveOperatorObservabilityCatalog } from '../../../lib/operator-observability-catalog.mjs'
export default async function ObservabilityCatalogPage(){const result=await loadOperatorWorkQueue();if(result.kind!=='ready')return <main className="portfolio-state"><span className="eyebrow">Catálogo de observabilidade</span><h1>Dados não confirmados.</h1><a href="/work-queue/overview">Voltar à central</a></main>;const view=deriveOperatorObservabilityCatalog(result.queue);const caps=Object.entries(view.capabilities);return <><header className="topbar"><a className="brand" href="/"><span className="brand-mark">C</span><span><strong>Contexto Ads</strong><small>Catálogo de observabilidade</small></span></a><div className="environment"><span />Somente leitura</div></header><main className="work-shell"><section className="work-hero"><div><span className="eyebrow">Cobertura explícita</span><h1>O que a central observa — e o que ainda não observa.</h1><p>O catálogo distingue capacidades disponíveis, fontes ainda indisponíveis e limites de execução.</p></div><a href="/work-queue/overview">Central de comando</a></section><section className="work-metrics"><article><span>Clientes autorizados</span><strong>{view.coverage.authorizedTenants}</strong></article><article><span>Itens atuais</span><strong>{view.coverage.currentItems}</strong></article><article><span>Clientes comparáveis</span><strong>{view.coverage.comparableTenants}</strong></article><article><span>Decisões de fonte</span><strong>{view.coverage.sourceDecisions}</strong></article></section><section className="work-list">{caps.map(([name,value])=><article className="work-item" key={name}><div className="work-item-head"><div><span>{value?'Disponível':'Não disponível'}</span><h3>{name}</h3></div></div></article>)}</section><div className="portfolio-boundary">Observabilidade não autoriza execução. Fontes ausentes não são simuladas e nenhuma escrita externa foi realizada.</div></main></>}
