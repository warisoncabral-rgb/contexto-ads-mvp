import { loadOperatorWorkQueue } from '../../../lib/operator-work-queue.mjs'
import { deriveOperatorSourceDecisionMatrix } from '../../../lib/operator-source-decision-matrix.mjs'
export default async function SourceMatrixPage(){const result=await loadOperatorWorkQueue();if(result.kind!=='ready')return <main className="portfolio-state"><span className="eyebrow">Matriz de fontes</span><h1>Dados não confirmados.</h1><a href="/work-queue/overview">Voltar à central</a></main>;const view=deriveOperatorSourceDecisionMatrix(result.queue);return <><header className="topbar"><a className="brand" href="/"><span className="brand-mark">C</span><span><strong>Contexto Ads</strong><small>Matriz de fontes</small></span></a><div className="environment"><span />Somente leitura</div></header><main className="work-shell"><section className="work-hero"><div><span className="eyebrow">Decisões persistidas</span><h1>Como cada fonte foi tratada entre os clientes.</h1><p>Incluída, adiada ou ignorada conforme os snapshots; sem inferir qualidade ou disponibilidade além do registro.</p></div><a href="/work-queue/overview">Central de comando</a></section><section className="work-list">{view.sources.map(row=><article className="work-item" key={row.source}><div className="work-item-head"><div><span>{row.tenantCount} cliente(s)</span><h3>{row.source}</h3></div></div><p>Incluída {row.included} · Adiada {row.deferred} · Ignorada {row.ignored}</p></article>)}</section><div className="portfolio-boundary">A matriz descreve decisões persistidas; não classifica a qualidade das fontes.</div></main></>}
