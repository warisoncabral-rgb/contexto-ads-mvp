import { loadOperatorWorkQueue } from '../../../lib/operator-work-queue.mjs'
import { deriveOperatorSnapshotAudit } from '../../../lib/operator-snapshot-audit.mjs'
export default async function SnapshotAuditPage(){const result=await loadOperatorWorkQueue();if(result.kind!=='ready')return <main className="portfolio-state"><span className="eyebrow">Snapshots</span><h1>Dados não confirmados.</h1><a href="/work-queue/overview">Voltar à central</a></main>;const view=deriveOperatorSnapshotAudit(result.queue);return <><header className="topbar"><a className="brand" href="/"><span className="brand-mark">C</span><span><strong>Contexto Ads</strong><small>Auditoria de snapshots</small></span></a><div className="environment"><span />Somente leitura</div></header><main className="work-shell"><section className="work-hero"><div><span className="eyebrow">Checkpoint diário</span><h1>Metadados dos snapshots persistidos.</h1><p>ID, data operacional, contagem e baseline são exibidos sem inferir autenticidade além do contrato validado.</p></div><a href="/work-queue/overview">Central de comando</a></section><section className="work-list">{view.snapshots.map(s=><article className="work-item" key={s.snapshotId}><div className="work-item-head"><div><span>{s.queueDate} · {s.calendarBasis}</span><h3>{s.itemCount} item(ns)</h3><small>{s.tenantId}</small></div></div><p>{s.baselineAvailable?`Baseline: ${s.previousQueueDate}`:'Sem baseline anterior'} · gerado em {s.generatedAt}</p></article>)}</section><div className="portfolio-boundary">A visão exibe metadados persistidos; não adiciona uma alegação independente de autenticidade.</div></main></>}
