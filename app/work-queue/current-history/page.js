import { loadOperatorWorkQueue } from '../../../lib/operator-work-queue.mjs'
import { deriveOperatorCurrentHistory } from '../../../lib/operator-current-history.mjs'
export default async function CurrentHistoryPage(){const result=await loadOperatorWorkQueue();if(result.kind!=='ready')return <main className="portfolio-state"><span className="eyebrow">Atual × histórico</span><h1>Dados não confirmados.</h1><a href="/work-queue/overview">Voltar à central</a></main>;const view=deriveOperatorCurrentHistory(result.queue);return <><header className="topbar"><a className="brand" href="/"><span className="brand-mark">C</span><span><strong>Contexto Ads</strong><small>Atual × histórico</small></span></a><div className="environment"><span />Somente leitura</div></header><main className="work-shell"><section className="work-hero"><div><span className="eyebrow">Separação explícita</span><h1>Pendência atual não é a mesma coisa que mudança histórica.</h1><p>Itens resolvidos permanecem apenas no histórico comparável e nunca voltam para a fila atual.</p></div><a href="/work-queue/overview">Central de comando</a></section><section className="work-metrics"><article><span>Pendências atuais</span><strong>{view.current.pendingCount}</strong></article><article><span>Mudanças históricas</span><strong>{view.history.changeCount}</strong></article><article><span>Resolvidas no histórico</span><strong>{view.history.resolvedCount}</strong></article><article><span>Entradas no histórico</span><strong>{view.history.enteredCount}</strong></article></section><div className="portfolio-boundary">Resolução histórica não é tratada como pendência atual e nenhuma conclusão adicional é inferida.</div></main></>}
