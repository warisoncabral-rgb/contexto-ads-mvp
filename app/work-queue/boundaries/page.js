import { loadOperatorWorkQueue } from '../../../lib/operator-work-queue.mjs'
import { deriveOperatorBoundaryLedger } from '../../../lib/operator-boundary-ledger.mjs'
export default async function BoundaryLedgerPage(){const result=await loadOperatorWorkQueue();if(result.kind!=='ready')return <main className="portfolio-state"><span className="eyebrow">Limites</span><h1>Dados não confirmados.</h1><a href="/work-queue/overview">Voltar à central</a></main>;const view=deriveOperatorBoundaryLedger(result.queue);return <><header className="topbar"><a className="brand" href="/"><span className="brand-mark">C</span><span><strong>Contexto Ads</strong><small>Ledger de limites</small></span></a><div className="environment"><span />Somente leitura</div></header><main className="work-shell"><section className="work-hero"><div><span className="eyebrow">Contrato operacional</span><h1>Todos os limites read-only em um único lugar.</h1><p>Esta visão verifica os flags já exigidos pelo contrato da fila; não amplia autorização.</p></div><a href="/work-queue/overview">Central de comando</a></section><section className="work-list">{view.entries.map(entry=><article className="work-item" key={entry.name}><div className="work-item-head"><div><span>{entry.matches?'Coerente':'Inconsistente'}</span><h3>{entry.name}</h3></div><strong>{String(entry.actualValue)}</strong></div></article>)}</section><div className="portfolio-boundary">Contrato completo: {view.allMatched?'coerente':'inconsistente'}. Nenhuma autorização foi ampliada.</div></main></>}
