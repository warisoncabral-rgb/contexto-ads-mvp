import { loadOperatorWorkQueue } from '../../../lib/operator-work-queue.mjs'
import { deriveOperatorEvidenceDensity } from '../../../lib/operator-evidence-density.mjs'
export default async function EvidenceDensityPage(){const result=await loadOperatorWorkQueue();if(result.kind!=='ready')return <main className="portfolio-state"><span className="eyebrow">Referências por item</span><h1>Dados não confirmados.</h1><a href="/work-queue/overview">Voltar à central</a></main>;const view=deriveOperatorEvidenceDensity(result.queue);return <><header className="topbar"><a className="brand" href="/"><span className="brand-mark">C</span><span><strong>Contexto Ads</strong><small>Densidade de referências</small></span></a><div className="environment"><span />Somente leitura</div></header><main className="work-shell"><section className="work-hero"><div><span className="eyebrow">Rastreabilidade sintática</span><h1>Quantas referências estão ligadas a cada pendência.</h1><p>Quantidade não significa qualidade, validade ou suficiência da evidência.</p></div><a href="/work-queue/overview">Central de comando</a></section><section className="work-metrics"><article><span>Referências totais</span><strong>{view.totalEvidenceRefs}</strong></article><article><span>Itens sem referência</span><strong>{view.itemsWithoutRefs}</strong></article></section><section className="work-list">{view.rows.map(row=><article className="work-item" key={row.workItemId}><div className="work-item-head"><div><span>{row.priority} · {row.owner}</span><h3>{row.evidenceRefCount} referência(s)</h3></div></div><p>{row.campaignId}</p></article>)}</section><div className="portfolio-boundary">Nenhuma suficiência, validade ou autenticidade foi inferida a partir da quantidade.</div></main></>}
