import { loadOperatorWorkQueue } from '../../../lib/operator-work-queue.mjs'
import { deriveOperatorPriorityDistribution } from '../../../lib/operator-priority-distribution.mjs'
const labels={critical:'Crítica',high:'Alta',normal:'Normal'}
export default async function PriorityPage(){const result=await loadOperatorWorkQueue();if(result.kind!=='ready')return <main className="portfolio-state"><span className="eyebrow">Prioridades</span><h1>Dados não confirmados.</h1><a href="/work-queue/overview">Voltar à central</a></main>;const view=deriveOperatorPriorityDistribution(result.queue);return <><header className="topbar"><a className="brand" href="/"><span className="brand-mark">C</span><span><strong>Contexto Ads</strong><small>Distribuição por prioridade</small></span></a><div className="environment"><span />Somente leitura</div></header><main className="work-shell"><section className="work-hero"><div><span className="eyebrow">Prioridade persistida</span><h1>Onde a carga atual está concentrada.</h1><p>Esta visão apenas conta as prioridades já atribuídas pelas regras determinísticas da fila.</p></div><a href="/work-queue/overview">Central de comando</a></section><section className="work-metrics">{view.distribution.map(x=><article key={x.priority}><span>{labels[x.priority]}</span><strong>{x.count}</strong><small>{x.tenantCount} cliente(s)</small></article>)}</section><div className="portfolio-boundary">Nenhum score de risco, prazo ou urgência adicional foi inferido.</div></main></>}
